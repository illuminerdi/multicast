# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/multicast.rb'

#Hoe.new('Multicast', Multicast::VERSION) do |p|
  # p.rubyforge_name = 'Multicastx' # if different than lowercase project name
  # p.developer('Joshua Clingenpeel', 'joshua.clingenpeel@gmail.com')
#end

# vim: syntax=Ruby
